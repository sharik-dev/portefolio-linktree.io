# Ce workflow est désactivé — sharik.fr est servi par le VPS via deploy-vps.yml
# GitHub Pages ne gère plus ce projet.
name: Deploy to GitHub Pages (disabled)

on:
  workflow_dispatch: {}

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use deploy-vps.yml instead"
